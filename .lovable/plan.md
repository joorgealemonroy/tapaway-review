

# Reorder Vibe Templates

Change the array order in `src/lib/vibeTemplates.ts` from current (pure, organic, obsidian, neon, vogue, elevate) to: **obsidian, organic, elevate, vogue, neon, pure**.

No other files need changes — the carousel renders from this array directly.

