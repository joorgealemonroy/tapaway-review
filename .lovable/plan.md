

# Reorder Vibe Templates

Update the `VIBE_TEMPLATES` array in `src/lib/vibeTemplates.ts` to match the requested sequence:

**Obsidian → Bloom → Organic → Vogue → Neon → Elevate → Pure**

Single file edit — reorder the template objects in the array. No other changes needed.

