
# Replace "Create yours — free" with Free Layout Link

## Change

**File:** `src/components/affiliate/AffiliateOnboarding.tsx`

Replace the "Create yours — free →" button (lines 109-114) below the Hub Showcase with a text link that scrolls down to the free Layout Templates section instead of navigating to signup.

**Before:**
```
Create yours — free →
```

**After:**
```
Or pick a free layout below ↓
```

This button will scroll to the Layout Templates section (similar to the existing scroll-to-showcase pattern already used in the hero). No other changes needed since we already have the "Want something free?" nudge text above the templates.
